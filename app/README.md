# Libreria de Python


# 
Flask + Jinja2.




## Intalación 

### Install Flask

Within the activated environment, use the following command to install Flask:

pip install Flask

### Create an environment

Create a project folder and a .venv folder within:

py -3 -m venv .venv

para poder hacer peticones 

